class data(object):
  
  def content(self):
    '''creates a dictionary/list/data type for storing data'''
    data = dictionary()
    
  def add_content(self, formated_data):
    '''adds formated data to content'''
    data.update(key:value)
    
  def format_raw_content(self ,raw_data_item):
    '''takes a single data item from raw data and outputs that data item in a desirable data type'''
    
    def data.type1_to_data.type2(datatype1):
      '''returns data type 2'''
      for i in datatype1:
        outputs data.type2
